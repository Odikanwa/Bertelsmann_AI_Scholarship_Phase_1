# Tuples - A DS for immutable ordered sequences of elements. Used for grouping elements that always go together

coordinates = 3.454566, 6.433355
print(coordinates)

# Unpacking tuples
longitude, latitude = coordinates
print(longitude, latitude)